#issue ispresent rewruitng the whole code below
# - name: install python package
#   ansible.builtin.dnf:
#     name: "{{ item }}"
#     state: present
#   loop:
#     - python3
#     - gcc
#     - python3-devel

# - name: install dependencies
#   ansible.builtin.pip:
#     requirements: requirements.txt
#     executable: pip3.9
#   args:
#     chdir: /app

- name: Install required system packages
  ansible.builtin.dnf:
    name:
      - python3
      - python3-pip
    state: present

- name: Install Python dependencies
  ansible.builtin.pip:
    requirements: requirements.txt
    executable: pip3
  args:
    chdir: /app
